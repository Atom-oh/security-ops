Review the current git diff (`git diff` / staged) for correctness bugs, security issues
(esp. AWS/IAM, secrets, injection), and reuse/simplification. Group by severity. Cite
file:line. For an FSI security tool, weight auth, crypto, data-handling, and IAM highest.
