# Runtime dependencies for the AgentCore container.
# Unit tests mock these out, so they are not required to run the test suite.
strands-agents>=0.1.0
bedrock-agentcore>=0.1.0
boto3>=1.34.0

# Dev / test
pytest>=8.0
moto[dynamodb]>=5.0
